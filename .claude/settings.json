{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PROJECT_DIR:-.}/scripts/bash_guard.py\"",
            "timeout": 10,
            "statusMessage": "명령 가드"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"${CLAUDE_PROJECT_DIR:-.}\" && jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f || exit 0; case \"$f\" in *.py) ./.venv/bin/ruff check --fix -q \"$f\"; ./.venv/bin/ruff format -q \"$f\";; esac; } >/dev/null 2>&1 || true",
            "timeout": 30,
            "statusMessage": "ruff"
          }
        ]
      }
    ]
  }
}
